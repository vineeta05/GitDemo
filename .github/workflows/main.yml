name: Node Docker CI/CD

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/basic-node-app:latest .

      - name: Push image to Docker Hub
        run: docker push ${{ secrets.DOCKER_USERNAME }}/basic-node-app:latest

      # OPTIONAL — Deploy to your server via SSH
      - name: Deploy to Server
        if: ${{ secrets.SERVER_IP != '20.224.162.42' }}
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            docker pull ${{ secrets.DOCKER_USERNAME }}/basic-node-app:latest
            docker stop nodeapp || true
            docker rm nodeapp || true
            docker run -d -p 3000:3000 --name nodeapp ${{ secrets.DOCKER_USERNAME }}/basic-node-app:latest

